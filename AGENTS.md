# AGENTS.md

## 1. 项目定位

本项目是一个 Windows 本地桌面工具，用于个人麻将学习和复盘。它通过手动框选雀魂游戏画面，周期性截取指定区域，识别用户自己的手牌、摸牌、宝牌指示牌和副露区，并给出以牌效为主的向听数、有效牌和切牌排序。

项目边界必须保持清晰：

- 不自动点击、出牌或操作游戏窗口。
- 不接入账号、网络协议或游戏内部数据。
- 不实现规避检测、隐藏式实战辅助等功能。
- 识别和分析均在本机完成，用户截图、配置和模板不提交到仓库。

## 2. 当前技术栈

- Python 3.12 及以上；若 Python 3.14 的图像依赖安装异常，优先使用 Python 3.12。
- PySide6：桌面界面、计时器、区域框选。
- mss：按物理屏幕坐标截图。
- OpenCV、NumPy、Pillow：图像裁剪、模板匹配和诊断图保存。
- unittest：当前测试框架。

安装与启动：

```powershell
python -m venv .venv
.\.venv\Scripts\python -m pip install -r requirements.txt
.\.venv\Scripts\python run_app.py
```

运行测试：

```powershell
.\.venv\Scripts\python -m unittest
```

截至 2026-07-18，测试共 176 项，全部通过。

## 3. 目录与职责

```text
app/
  main_window.py       主窗口、配置交互、识别循环、结果展示
  screen_select.py     全屏透明框选层和 DPI 坐标换算
recognizer/
  capture.py           mss 屏幕截图
  config.py            配置数据类、默认值、JSON 读写
  geometry.py          屏幕矩形数据结构
  models.py            识别结果和匹配结果数据结构
  recognizer.py        牌区裁剪、等宽切牌、置信度检查、诊断图
  table_state.py       场风、局数、自风和四家点数的有限模板识别
  table_template_builder.py  从已标注截图裁取本地桌况模板
  template_builder.py  从完整截图按当前牌区配置生成模板
  templates.py         模板加载和 OpenCV 匹配
mahjong/
  tiles.py             34 种牌编码、别名和计数转换
  shanten.py           一般形、七对子、国士无双向听数
  analyzer.py          有效牌计算和切牌排序
data/
  config.example.json  可提交的配置示例
  config.json          本机实际配置，已忽略
  templates/           用户模板，图片已忽略，仅提交 .gitkeep
  table_templates/     用户桌况模板，图片已忽略，仅提交 .gitkeep
  debug/               失败诊断图，已忽略
tests/                 配置、模板裁剪、匹配和牌效单元测试
run_app.py             Windows DPI 感知设置及程序入口
```

## 4. 核心数据与约定

### 4.1 牌名

标准牌名为：

- 数牌：`1m` 至 `9m`、`1p` 至 `9p`、`1s` 至 `9s`。
- 字牌：`east south west north white green red`。
- 赤五模板可命名为 `5mr 5pr 5sr`；进入牌效计算前分别归一化为 `5m 5p 5s`。

模板文件名的 stem 就是识别结果中的牌名。当前本机模板已经补齐 34 种普通牌及 3 种赤五，共 37 个模板，但这些用户生成图片不会进入 Git。

### 4.2 配置

实际配置位于 `data/config.json`。主要字段：

- `game_region`：物理屏幕坐标，包含 `left/top/width/height`。
- `layout.*_region`：相对于 `game_region` 的 0 至 1 比例坐标。
- `hand_tile_count`：手牌区当前可见暗牌张数。
- `draw_tile_count`：独立摸牌区张数，只允许 0 或 1。
- `meld_tile_count`：副露区当前可见牌张数。
- `open_meld_count`：已完成副露组数，范围 0 至 4；牌效分析直接使用此值。
- `melds`：结构化副露组列表；每组记录 `kind`，每张可见牌记录相对副露总区的 `region`、`orientation` 和 `stack_level`。
- `opponent_melds`：屏幕视角 `right/across/left` 三家的副露区域、结构化组与逐张牌槽。
- `rivers`：`self/right/across/left` 四家牌河；每张当前仍可见的牌记录相对牌河总区的区域、行列、方向和立直横牌状态。被鸣走的历史弃牌不配置。
- `table_state`：可选的场风/局数、自风区域，以及四家点数区域和各自旋转归一方向；内部仍按 `self/right/across/left` 保存。
- `dora_tile_count`：画面中当前实际翻开的宝牌指示牌张数，UI 限制为 1 至 5；现阶段在总宝牌区内等宽切分。
- `recognition.threshold`：最低匹配置信度，默认 0.78。
- `capture_interval_seconds`：连续识别间隔，默认 0.75 秒。

常用牌数配置：

- 闭门摸牌后：`13 1 0 0`。
- 一组副露后待切：`10 0 3 1`。
- 一组杠、杠后补牌：`10 1 4 1`（副露区应计入杠的 4 张可见牌；叠放样式仍需按画面准确框选）。
- 两组副露后待切：`7 0 6 2`。

### 4.3 识别结果

`RecognitionResult` 包含：

- `hand`：手牌区识别结果。
- `draw`：独立摸牌，未配置时为 `None`。
- `dora_indicators`：宝牌指示牌列表。
- `meld_tiles`：副露区从左到右的可见牌。
- `melds`：逐组、逐张的结构化识别结果，可保留单张未知状态和最佳候选。
- `opponent_melds`：右家、对家、左家的结构化副露识别结果。
- `rivers`：四家牌河逐张结果，包含稳定行列、立直状态、未知状态和候选。
- `confidence`：手牌、摸牌和宝牌指示牌这些核心区域的平均匹配分数；副露与牌河不会污染此值。
- `matches`：逐张牌的名称和分数。
- `table_state`：可独立未知的场风与局数、自风、四家点数；不污染核心牌面平均置信度。

手牌、摸牌或宝牌低于阈值时，整次识别会抛出 `RecognitionError`；副露或牌河低于阈值时仅将对应牌槽记为未知并继续牌效分析。

## 5. 当前实现进度

### 已完成

- Windows 桌面主窗口和定时识别循环。
- 游戏画面、手牌、摸牌、宝牌和副露区的手动框选及持久化。
- Windows DPI 感知和 Qt 逻辑坐标到物理像素坐标的换算，已验证 1920x1080 游戏画面可正确框选。
- 工具窗口与游戏区域重叠时，截图前自动隐藏；无重叠时不隐藏，以减少连续识别闪烁。
- 按配置张数等宽切分牌区。
- 本地模板加载、自身截图半自动裁模板、最低置信度校验。
- 直接匹配与“去边缘核心滑动匹配”取最高分，能够容忍少量裁剪偏移。
- 失败时保存完整区域叠加图和逐张裁剪图到 `data/debug/last_failed/`。
- 闭门 13/14 张牌的一般形、七对子、国士向听数计算。
- 按“切牌后向听数升序、有效牌枚数降序”给出推荐。
- 副露牌的画面识别和文字展示。
- 副露后的牌效分析：一般形向听支持 `open_meld_count`，开放手牌禁用七对子和国士分支；UI 已支持 10/11、7/8 张暗牌，并可扩展到 4/5、1/2 张。
- 副露牌面识别已与核心牌效分析解耦：副露低置信度时显示具体警告，但不再阻断手牌、摸牌的分析；手牌或摸牌低置信度仍会阻断整次结果。
- 结构化副露后端：支持每组类型、独立牌槽、横置旋转归一、重叠/叠放矩形、组间空隙，以及逐张失败降级；旧配置继续走整区等宽回退。
- 结构化副露 UI：可输入每组类型、每张实际朝向和叠放层，随后逐张框选实际可见牌面；全部完成后原子保存，途中取消保留旧配置。
- 配置合法性检查：识别前检查副露组数、结构类型、牌槽数量/区域/方向、叠放层和置信度阈值。
- 候选诊断：模板库可返回按分数排序的最佳/次佳候选；核心错误包含区域与序号，副露逐张结果保留候选。
- 识别阈值可在 UI 中设置并持久化，范围限制为 0.50 至 0.99。
- 连续识别稳定化：只比较激活的手牌、摸牌、宝牌和副露关键区；静止画面复用结果并跳过模板匹配，新结果连续三次一致后发布。
- 自动牌数状态：将手牌区作为最大 13 个固定槽位，通过独立的牌面占位检测自动判断 `13/10/7/4/1` 张手牌、独立摸牌 `0/1` 和副露组数；不确定时回退手动配置，低置信度牌不会被误当作空槽。
- 自动待切状态：支持副露后连续 `11/8/5/2` 张暗牌，并兼容末槽或独立摸牌布局；白色空槽通过牌面与图案双特征排除，合法最大槽位中的冲突/动画状态会硬失败而非静默回退错误牌数。
- 白板占位复核：明亮无图案槽位不会直接当成空槽；只有白板模板分数至少 0.95、达到全局阈值且明显领先次佳候选时才判为真实白板。真实失败截图中两张白板分别以 0.999/1.000 通过复核，闭门 13 张恢复正确；非白板高分候选仍不能把纯白占位变成有牌槽。
- 游戏画面切牌建议浮层：稳定结果发布后显示首选切牌、向听、有效牌和备选；浮层鼠标穿透，并在 Windows 上优先从屏幕捕获中排除。
- 建议浮层位置可调整：主窗口可进入拖动模式，松开后按游戏区域相对坐标持久化；正常模式恢复鼠标穿透。
- 摸牌前展示会将分析层的 `discard="-"` 哨兵渲染为“等待进张”，不会再显示“切 -”；完成和牌形时也不会继续给出切牌标题。
- 副露类型识别首版：在已框选并配置牌槽的副露总区内，根据可靠牌面自动判断吃、碰、明杠；仅在叠放或牌背几何明确时判断加杠、暗杠，不确定形态保留 `unknown`。自动牌数仍是自己固定面子数的牌效权威。
- 手动“识别一次”仍立即发布并作为稳定基线；错误帧不推进稳定计数，存在旧结果时明确标记为可能过时。
- 其他三家副露：按屏幕方位分别配置总区、组、独立牌槽、朝向和叠放，逐张失败只降级该槽。
- 四家牌河：分别配置总区和当前可见牌槽，支持不连续行列、不完整末行及立直横牌；逐张未知不会误扣有效牌余量。
- 杠宝牌指示器：UI 可设置当前实际翻开的 1 至 5 张，后端按总区从左到右等宽识别。
- 全桌可见牌汇总：可靠识别的自己/他家副露、四家牌河和宝牌指示牌会用于修正有效牌剩余枚数，未知槽不参与扣除。
- 牌河低置信度诊断包含方位、顺序、稳定 `row/column`、最佳/次佳候选和阈值；诊断文本与分数变化不影响稳定键。
- Git 仓库已初始化并推送到 `main`：<https://github.com/nicjessica/majsoul-screen-recognizer>。
- 阶段感知动作决策基础层：待摸状态不会持续显示切牌；待切状态可生成切牌、默听与条件立直候选；吃、碰、明杠、暗杠、加杠具备严格合法性校验。当前只做向听与有效牌的相对比较，不伪造胡率百分比或完整打点。

### 部分完成

- 赤五：模板和牌名归一化已支持，牌效中按普通五处理；尚未在 UI 中单独显示赤宝牌价值，也未参与打点判断。
- 副露：底层与 UI 已能手动表达吃、碰、明杠、暗杠、加杠、横置与牌组边界；尚未自动判断边界和类型。
- 连续识别：已完成关键区域变化检测和三次一致发布；32×32 灰度快照的 MAD 阈值 2.0 尚需真实游戏动画联调。
- 动作建议：新增独立 `RiverEventTracker`，只消费连续发布的稳定 `RecognitionResult`，按对手方位和稳定行列产生单调 `event_id` 的最新弃牌事件；第一帧只建基线，单帧多槽变化、可靠牌改名、低置信未知恢复和诊断分数抖动均不触发，同一事件不重复发布。鸣牌窗口始终包含“跳过”，再列出严格合法的吃、碰、明杠；明杠因岭上牌未知只标记谨慎考虑，不与普通候选硬排。手动单次识别只重建事件基线，配置失效或识别错误会关闭旧鸣牌窗口。立直缺少点棒或余牌输入时只显示条件式提示。
- 建议浮层动作化文案：标题统一为“建议操作”，并严格按阶段显示动作。待摸阶段显示“跳过（等待进张）”；鸣牌窗口按“吃、碰、杠、跳过”固定顺序只列合法动作；自己待切且切后听牌时显示切牌与合法立直，点棒或余牌未知时标记“立直（条件式）”。不会把五类动作脱离时机无条件并列。
- 牌河框选 UI 使用玩家习惯称谓并提供四个直接按钮：我的、上家、下家、对家；内部兼容键不变，固定映射为 `self/left/right/across`。
- 桌况识别首版基础设施：可分别框选中心场风/局数、我的自风和四家点数，点数显式配置旋转归一方向；使用独立 `data/table_templates` 有限整值模板，支持从已标注截图裁取 `round_east_1`、`wind_north`、`score_25000` 等模板。各字段低分或缺模板时独立软降级，不阻断手牌分析；可靠值进入三帧稳定键，场风、自风和自己的点数进入动作上下文。未知风位不再默认伪装成东风。

### 尚未完成或当前限制

- 有效牌余量已扣除可靠识别的自己/他家副露、四家当前可见牌河和宝牌指示牌；尚未识别的牌河牌、低置信度未知牌及其他画面外信息不会扣除，因此仍不是完整场况余量。
- 模板裁剪器只生成手牌区和可选摸牌区模板，不从副露区生成模板。
- 未配置结构化 `melds` 的旧配置仍使用简单等宽切分，无法处理横置牌宽度、杠牌叠放和不同间距。
- 核心手牌、摸牌或宝牌指示牌只要一张低于阈值仍会整帧失败；副露和牌河已经逐张软降级。宝牌是否也应软降级需要产品决策。
- 已支持四家牌河和他家副露的手动结构化牌槽识别；尚未识别巡目、点棒、场风、自风或危险度。
- 尚未完成完整役种、番符点数、防守和局面决策；现有价值估计只保守统计确定的立直、役牌和已知宝牌下限。
- 没有固定真实截图回归测试；当前图像测试主要是合成图。
- 桌况识别不是通用 OCR：只能识别本机已经采样的完整值模板。当前提供的单张截图只覆盖东 1 局、北家视角和 25000 点，尚未覆盖东/南各局、四种自风、不同及负数点数、滚分动画；未采样值必须保持未知。
- 没有打包为可直接运行的 Windows `.exe`。

## 6. 实际调试中学到的知识

### 6.1 分辨率与 DPI

雀魂内部设置的 1920x1080 与 Windows 桌面缩放是两个维度。Windows 125% 缩放会让 Qt 返回逻辑坐标，而 mss 使用物理像素；如果不转换，1920x1080 画面可能被错误记录成约 1536x864，牌区随之偏移。当前入口先启用 DPI awareness，框选器再乘 `devicePixelRatio()`。

修改框选或截图代码时必须同时验证：

- Windows 缩放 100% 和 125%。
- 游戏分辨率 1920x1080。
- `game_region` 保存尺寸是否与实际游戏画面一致。
- `full_capture_regions.png` 中彩色框是否贴合真实牌区。

### 6.2 框选精度决定模板质量

模板和实时识别必须使用相同的游戏分辨率、窗口尺寸、皮肤及近似牌区边界。框选应覆盖完整牌面，但尽量少带桌面背景、阴影和相邻牌。只看区域位置正确还不够，逐张切分边界也必须正确。

诊断顺序：

1. 看 `full_capture_regions.png`，确认大区域位置。
2. 看 `hand_*.png`、`draw_*.png`、`dora_*.png`、`meld_*.png`，确认每张图只含一张完整牌。
3. 若切分正确但分数低，检查模板是否来自相同显示条件。
4. 再考虑微调阈值；不要先用大幅降低阈值掩盖区域或模板问题。

### 6.3 工具窗口与闪烁

如果工具窗口覆盖游戏画面，它会进入截图并污染识别。当前仅在窗口与 `game_region` 相交时隐藏 180 ms 后截图。连续闪烁通常来自窗口反复隐藏和显示，不是因为副露后暗牌数量变成 11/8；将工具移到游戏区域外可避免隐藏。

从 `cmd.exe` 启动时，启动控制台同样可能覆盖游戏并污染宝牌等核心区域；入口会在应用运行期间最小化该控制台，退出后恢复。诊断图若出现命令行文字，应先排除此类窗口遮挡，而不是降低识别阈值。

### 6.4 副露后的牌数状态

副露后正常情况下不会产生独立摸牌：鸣牌完成后直接打出一张，因此常见配置是 `10 0 3 1` 或 `7 0 6 2`。杠后会补牌，才可能出现独立摸牌区为 1。四个数字依次表示手牌区张数、摸牌张数、副露可见张数和副露组数；牌数配置必须描述当前画面真实可见牌。

### 6.5 模板与赤五

赤五画面应使用独立模板 `5mr/5pr/5sr`，否则颜色和图案差异可能降低置信度。牌效层会把赤五归一化成普通五，这对向听和进张正确，但会丢失赤宝牌身份。未来做保留赤五偏好或打点时，必须在分析数据中另外保存赤牌标记。

### 6.6 副露识别失败的降级策略

副露区受横置、叠放和组间空隙影响，等宽切分的置信度不应决定核心牌效是否可用。当前仅手牌、摸牌和宝牌识别失败会抛出整帧错误；副露单张低于阈值时，该张记录未知状态与最佳候选，其他成功副露牌继续保留，并使用配置中的 `open_meld_count` 进行牌效分析。副露失败匹配不计入置信度。

### 6.7 结构化副露坐标

结构化副露的每个牌槽使用相对整个 `meld_region` 的 0 至 1 坐标。横置牌必须在匹配前按 `rotated_cw` 或 `rotated_ccw` 旋转归一；加杠等叠放可用相互重叠的牌槽和不同 `stack_level` 表达。核心平均置信度只统计手牌、摸牌和宝牌，副露按组单独计算，避免副露布局变化导致核心置信度不可比较。

### 6.8 牌河与杠宝牌语义

- 牌河槽表示当前截图中仍物理可见的牌，不是历史弃牌记录；空位和已被鸣走的位置直接跳过，行列允许不连续。
- 立直横牌仍只计一张，`is_riichi` 记录状态，`orientation` 负责旋转归一；未知低分槽显示但绝不能用最佳候选代扣有效牌。
- `dora_tile_count` 表示当前已经翻开的指示牌数量，不根据杠数提前推断。动画尚未翻开时不要增加；实际翻开后再设为 2 至 5。
- 宝牌指示牌本身属于已知可见牌并参与余量扣除，但现阶段不把指示牌转换成实际宝牌，也不据此做打点判断。
- 真实画面若证明多张宝牌有不等间距或末端留白，等宽方案应判定为不可靠并升级独立牌槽；不要通过降低阈值掩盖偏裁。

## 7. 下一步计划（按优先级）

### P1：扩展全桌可见牌识别

方位统一使用屏幕视角 `self/right/across/left`，不随场风、自风或亲家变化；每张牌的视觉方向独立配置，不按方位隐式旋转。

1. 已完成首版：新增其他三家的副露区域、结构化副露结果、逐张软降级、诊断图、稳定器关键区，以及 UI 分家框选、结构配置和展示。
2. 已完成第一版：新增四家牌河区域与逐张牌槽，UI 可分别框选区域并按行列配置当前仍可见的牌，支持不连续空位、不完整末行、立直横置牌、逐张未知状态、稳定器、结果展示和可见牌汇总。被鸣走的历史弃牌不配置、不参与可见牌扣除。
3. 已完成第一版：UI 可设置画面中实际翻开的 1 至 5 张宝牌指示牌，现有宝牌区按张数等宽切分并保持从左到右顺序；已覆盖初始 1 张、开杠后 2 张和最多 5 张回归。框选区域必须只包含已翻开的正面牌；若真实画面存在非等宽间距，再升级为独立牌槽。
4. 已完成当前来源接线：`analyze_hand(..., visible_tiles=())` 扣除可靠的自己/他家副露、四家当前可见牌河与宝牌指示牌，并修正候选切牌自身的可见计数；逐张未知牌不会误扣余量。
5. 已完成诊断补强：牌河低分错误可直接按方位和 `row/column` 定位，候选/错误文本抖动不改变稳定键。
6. 已完成自动化验收：新增全桌组合流水线测试，在同一合成画面同时启用三家副露、四家牌河、立直牌、2 张宝牌、手牌和摸牌，验证固定顺序、未知软降级、核心置信度隔离、每个物理牌槽只汇总一次及牌效扣减；测试保留合法的同名可见牌，不把“无重复汇总”误解为按牌名去重。
7. 待真实验证：1920×1080、Windows 100%/125% 缩放下逐一核对三家副露、四家牌河、立直横牌和 1/2/5 张宝牌指示牌。

### P0：完善副露后的牌效输入

1. 已完成：一般形向听算法增加 `open_meld_count`，从初始已完成面子数开始搜索。
2. 已完成：`analyze_hand()` 接受暗牌列表和副露组数，支持暗牌张数 13/14、10/11、7/8、4/5、1/2。
3. 已完成：开放手牌时禁用七对子和国士分支。
4. 已完成：UI 使用显式 `open_meld_count` 调用分析，副露可见牌不混入暗牌。
5. 已完成：增加一组、两组副露的向听、有效牌和切牌排序测试。
6. 已完成：配置与 UI 增加显式副露组数；旧配置加载时根据暗牌数迁移，之后不再依赖暗牌数推导。

验收结果：`10 0 3 1` 和 `7 0 6 2` 对应的暗牌张数已通过确定牌例测试，可给出正确向听、有效牌与推荐；仍需真实游戏截图联调确认完整识别链路。

### P1：建立可靠的副露结构识别

1. 已完成后端：结构化副露组支持 `chi/pon/minkan/ankan/kakan/unknown`、独立牌槽、朝向和叠放层。
2. 已完成后端与合成测试：牌槽可跨过组间空隙、使用不同宽度并相互重叠，横置牌会旋转归一。
3. 已完成：UI 增加每组类型、每张方向/叠放层和逐张手动框选；全部完成后一次性保存，取消不覆盖旧结构。
4. 已完成：识别结果保留逐组逐张 `melds`，并兼容现有 `meld_tiles/meld_error`。
5. 待真实验证：1920×1080 下的吃、碰、明杠、加杠、横置三位置和两组副露；暗杠无样本时明确标记。
6. 待办：基于手动结构生成几何初值并做局部边界微调；在可靠性验证前，手动牌槽仍是权威配置。

### P1：识别稳定性与诊断

1. 已完成：连续识别比较激活关键区域的灰度差异，画面未变化时复用结果并跳过模板匹配。
2. 已完成首版：新结果连续三次一致后发布；错误帧不推进，配置或模板变化统一清空缓存。
3. 已完成：低置信度时展示具体区域、序号、最佳候选和次佳候选；副露逐张保留候选状态，牌河额外显示稳定行列。
4. 已完成：增加可调阈值 UI，限制为 0.50 至 0.99 并持久化。
5. 用脱敏后的固定截图建立回归夹具，覆盖闭门、赤五、副露和杠后补牌。

### P2：提高牌效数据准确度

1. 已完成第一版：有效牌剩余枚数扣除可靠识别的自己/他家副露、四家当前可见牌河和宝牌指示牌。
2. 保留赤五身份，在同牌效时减少误切赤五。
3. 增加宝牌指示牌到实际宝牌的转换和简单价值排序。
4. 明确区分“理论有效牌种类”“理论剩余枚数”和“已知可见牌修正后枚数”。

### P2：易用性与发布

1. 用下拉框或步进控件替代四个数字的自由文本输入，并提供常见状态预设。
2. 增加配置导入、导出和恢复默认值。
3. 增加模板管理页：缺失牌、重复牌、尺寸异常和自匹配置信度检查。
4. 使用 PyInstaller 生成 Windows 可执行文件，并在干净机器验证启动、截图权限和 DPI。
5. 补充 README 中的副露限制、赤五说明和故障排查步骤。

## 8. 开发与修改约定

- 保持现有 `app / recognizer / mahjong` 分层；UI 不直接实现图像算法或向听算法。
- 新增结构化数据优先使用 dataclass，避免用松散字典在模块间传递。
- 配置新增字段时必须给出默认值，并保证旧 `data/config.json` 可加载。
- 所有屏幕绝对坐标使用物理像素；牌区内部位置使用相对游戏区域的比例。
- 图像颜色约定：mss 输出经 Pillow 转为 RGB；OpenCV 模板内部转为 BGR。
- 不提交 `data/config.json`、`data/debug/`、用户截图或 `data/templates/*.png`。
- 修改向听或有效牌逻辑必须增加确定牌例测试。
- 修改裁剪、匹配或 DPI 逻辑必须至少运行全部测试，并通过真实截图检查诊断图。
- 不以降低全局置信度阈值作为识别问题的首选修复。
- 不加入自动操作游戏的代码。

## 9. 当前 Git 状态（2026-07-18 交接）

- 远程仓库：<https://github.com/nicjessica/majsoul-screen-recognizer>
- 默认分支：`main`
- 当前轮修复自动牌数检测对白板的误判：仅在严格白板模板门槛和候选领先条件下把无图案槽恢复为白板；真实失败诊断图已离线重放为 13 张闭门手牌。补充对家点数区域覆盖保存、`rotated_180` 和 `table_score_across.png` 诊断裁图测试；全量 176 项测试通过。提交与推送状态仍须在新任务开始时用 `git status --short --branch`、`git log -8 --oneline` 和远端哈希重新核对，不要把本段当作永久事实。
- 桌况识别下一步：用真实 1920×1080、Windows 100%/125% 截图逐步采集东/南 1 至 4 局、四种自风、不同四家点数、负分和滚分动画模板；不同值样本不足前不得宣称通用数字 OCR 或把低分候选用于决策。本场数、立直棒和牌山余量尚未接入。
- 当前限制与下一最高优先级：`RiverEventTracker` 严格要求上游用“上一稳定结果中坐标完全不存在、下一结果中出现可靠牌”表达空槽进牌；现有手工牌河配置若预先包含未来槽位，只会得到 `name=None` 的低置信未知，无法可靠区分真实空槽与识别抖动，因此不会触发。下一轮应为牌河新增独立占位检测或明确的 empty 状态，使识别层只输出当前有牌槽，再用真实 1920×1080、Windows 100%/125% 截图验证事件链路；不得把低置信未知直接当空槽。
- 真实 Windows/雀魂截图仍未纳入仓库；不要提交 `data/config.json`、模板、截图或 `data/debug/`。

后续修改建议在独立分支完成，提交前至少执行：

```powershell
git status --short
.\.venv\Scripts\python -m unittest
```

## 10. 多 Agent 协同

本项目的多 Agent 角色、文件所有权、接口冻结、测试责任、逐轮 Git 提交与 GitHub 推送、上下文压缩交接和持续迭代流程统一遵循 [`MULTI_AGENT.md`](MULTI_AGENT.md)。核心规则是“1 个主 Agent + 最多 3 个专项 Agent”“单文件单负责人”；主 Agent 负责整体架构、功能路线、`app/main_window.py` 集成和最终验收，每轮通过验收后提交、推送到远程仓库并标注本轮内容，在触发自动上下文压缩后使用 `context-handoff` 技能完成当前轮收尾与新任务交接，并在已获得持续推进授权时主动规划和派发下一轮功能。
